# Learnings — the build loop's memory

One curated finding per file; this index is the hint surface. Load it, then read only the findings that bear on the change at hand. Generated by `render-learnings-index.sh` — do not hand-edit. Contract: docket-convention, "Learnings ledger".

## testing

- [pytest-shim-and-venv-provisioning](pytest-shim-and-venv-provisioning.md) — Run the suite as `uv run python -m pytest` after `uv sync --extra dev` — a bare `pytest` resolves to a pyenv shim that loads a crashing global deepeval plugin. · also: environment, tooling ⟨needs promotion⟩
